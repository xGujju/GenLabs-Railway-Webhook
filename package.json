{
  "name": "line-webhook",
  "version": "1.0.0",
  "type": "module",
  "description": "LINE Messaging API webhook server",
  "main": "server.js",
  "scripts": {
    "start": "PORT=5000 node src/cli/webhook.js",
    "dev": "PORT=5000 node src/cli/webhook.js",
    "start:all": "./run.sh",
    "worker:once": "node src/cli/worker-once.js",
    "worker:loop": "node src/cli/worker-loop.js",
    "test": "node --test"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "supertest": "^7.1.1"
  }
}
